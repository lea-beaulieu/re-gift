{{!--
{{#if userInSession}} 


<h1> Transactions status</h1>


<h2> Tradings I have requested </h2>
<form action="/transactionsstatus" method="POST">

    {{#each transactionsIaskedfor}}
    <p>Gift I would like to have : <a href="/mygifts/{{this.giftA.id}}">{{this.giftA.name}}</a></p>
    <p>Gift I proposed: <a href="/mygifts/{{this.giftB.id}}">{{this.giftB.name}}</a></p>
    <p>On : {{this.createdAt}}</p>
    {{/each}}

</form>

<h2> Pending tradings requested by others users </h2>

<form action="/transactionsstatus" method="POST">

    {{#each transactionsIamaskedfor}}
    <p>Gift I have : <a href="/mygifts/{{this.giftA.id}}">{{this.giftA.name}}</a></p>
    <p>Gift suggested to me: <a href="/mygifts/{{this.giftB.id}}">{{this.giftB.name}}</a> from {{this.giftB.brand}} (category : {{this.giftB.category}})</p>
    <p>On : {{this.createdAt}}</p>
    <p>Status: {{this.statut}}</p>

{{!-- 
    <label>
      Answer now :

      <select multiple name="statut">
        <option value="accepted">accepted</option>
        <option value="declined">declined</option> 
      </select>

    </label>
    <button>Submit my choice</button>
--}}

    {{/each}}

</form>


<a href="/profile">Return to my profile</a>
{{else}}
    <p>Connectez-vous !</p>
{{/if}} 

--}}